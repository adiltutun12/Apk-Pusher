# Apk-Pusher-
A lightweight local web tool for deploying signed APKs to Android set-top boxes over ADB. Drag &amp; drop your APK, hit push — it handles connect, root, remount, push and reboot automatically.
